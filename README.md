# homebrew-grove

Homebrew tap for [Grove](https://github.com/henrypldev/grove) - mobile terminal server for Claude Code.

## Installation

```bash
brew tap henrypldev/grove
brew install grove
```

## Usage

```bash
grove              # Start server interactively
grove -b           # Start in background
grove --stop       # Stop background server
grove --help       # Show help
```

## Updating

```bash
brew update
brew upgrade grove
```
